== INSTALL

Should install out of the box, just requires ruby & rake

> git clone https://delonnewman@github.com/delonnewman/dotfiles.git
> cd dotfiles
> rake install

or

just need bash & curl

> bash < <(curl -s https://raw.github.com/delonnewman/dotfiles/master/bin/fetch)
